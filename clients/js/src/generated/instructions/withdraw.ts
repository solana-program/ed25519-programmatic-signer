/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core';
import { NONCE_PROGRAM_ADDRESS } from '../programs';

export const WITHDRAW_DISCRIMINATOR = 2;

export function getWithdrawDiscriminatorBytes(): ReadonlyUint8Array {
    return getU8Encoder().encode(WITHDRAW_DISCRIMINATOR);
}

export type WithdrawInstruction<
    TProgram extends string = typeof NONCE_PROGRAM_ADDRESS,
    TAccountAuthority extends string | AccountMeta<string> = string,
    TAccountNonceAccount extends string | AccountMeta<string> = string,
    TAccountDestination extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountAuthority extends string
                ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
                : TAccountAuthority,
            TAccountNonceAccount extends string ? WritableAccount<TAccountNonceAccount> : TAccountNonceAccount,
            TAccountDestination extends string ? WritableAccount<TAccountDestination> : TAccountDestination,
            ...TRemainingAccounts,
        ]
    >;

export type WithdrawInstructionData = { discriminator: number; lamports: bigint };

export type WithdrawInstructionDataArgs = { lamports: number | bigint };

export function getWithdrawInstructionDataEncoder(): FixedSizeEncoder<WithdrawInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', getU8Encoder()],
            ['lamports', getU64Encoder()],
        ]),
        value => ({ ...value, discriminator: WITHDRAW_DISCRIMINATOR }),
    );
}

export function getWithdrawInstructionDataDecoder(): FixedSizeDecoder<WithdrawInstructionData> {
    return getStructDecoder([
        ['discriminator', getU8Decoder()],
        ['lamports', getU64Decoder()],
    ]);
}

export function getWithdrawInstructionDataCodec(): FixedSizeCodec<
    WithdrawInstructionDataArgs,
    WithdrawInstructionData
> {
    return combineCodec(getWithdrawInstructionDataEncoder(), getWithdrawInstructionDataDecoder());
}

export type WithdrawInput<
    TAccountAuthority extends string = string,
    TAccountNonceAccount extends string = string,
    TAccountDestination extends string = string,
> = {
    /** Authority stored in the nonce account */
    authority: TransactionSigner<TAccountAuthority>;
    /** Nonce account to withdraw from */
    nonceAccount: Address<TAccountNonceAccount>;
    /** Account receiving the lamports */
    destination: Address<TAccountDestination>;
    lamports: WithdrawInstructionDataArgs['lamports'];
};

export function getWithdrawInstruction<
    TAccountAuthority extends string,
    TAccountNonceAccount extends string,
    TAccountDestination extends string,
    TProgramAddress extends Address = typeof NONCE_PROGRAM_ADDRESS,
>(
    input: WithdrawInput<TAccountAuthority, TAccountNonceAccount, TAccountDestination>,
    config?: { programAddress?: TProgramAddress },
): WithdrawInstruction<TProgramAddress, TAccountAuthority, TAccountNonceAccount, TAccountDestination> {
    // Program address.
    const programAddress = config?.programAddress ?? NONCE_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        authority: { value: input.authority ?? null, isWritable: false },
        nonceAccount: { value: input.nonceAccount ?? null, isWritable: true },
        destination: { value: input.destination ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('authority', accounts.authority),
            getAccountMeta('nonceAccount', accounts.nonceAccount),
            getAccountMeta('destination', accounts.destination),
        ],
        data: getWithdrawInstructionDataEncoder().encode(args as WithdrawInstructionDataArgs),
        programAddress,
    } as WithdrawInstruction<TProgramAddress, TAccountAuthority, TAccountNonceAccount, TAccountDestination>);
}

export type ParsedWithdrawInstruction<
    TProgram extends string = typeof NONCE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** Authority stored in the nonce account */
        authority: TAccountMetas[0];
        /** Nonce account to withdraw from */
        nonceAccount: TAccountMetas[1];
        /** Account receiving the lamports */
        destination: TAccountMetas[2];
    };
    data: WithdrawInstructionData;
};

export function parseWithdrawInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 3,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { authority: getNextAccount(), nonceAccount: getNextAccount(), destination: getNextAccount() },
        data: getWithdrawInstructionDataDecoder().decode(instruction.data),
    };
}
