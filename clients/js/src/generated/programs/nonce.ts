/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionWithAccounts,
    containsBytes,
    extendClient,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
    SolanaError,
    type Address,
    type ClientWithRpc,
    type ClientWithTransactionPlanning,
    type ClientWithTransactionSending,
    type ExtendedClient,
    type GetAccountInfoApi,
    type GetMultipleAccountsApi,
    type Instruction,
    type InstructionWithData,
    type ReadonlyUint8Array,
} from '@solana/kit';
import {
    addSelfFetchFunctions,
    addSelfPlanAndSendFunctions,
    type SelfFetchFunctions,
    type SelfPlanAndSendFunctions,
} from '@solana/kit/program-client-core';
import { getNonceCodec, type Nonce, type NonceArgs } from '../accounts';
import {
    getAdvanceInstruction,
    getInitializeInstruction,
    parseAdvanceInstruction,
    parseInitializeInstruction,
    type AdvanceInput,
    type InitializeInput,
    type ParsedAdvanceInstruction,
    type ParsedInitializeInstruction,
} from '../instructions';

export const NONCE_PROGRAM_ADDRESS =
    'Noncediea1fH12usShuQAz28UhgAeuE5Maf32LsMUQB' as Address<'Noncediea1fH12usShuQAz28UhgAeuE5Maf32LsMUQB'>;

export enum NonceAccount {
    Nonce,
}

export function identifyNonceAccount(account: { data: ReadonlyUint8Array } | ReadonlyUint8Array): NonceAccount {
    const data = 'data' in account ? account.data : account;
    if (data.length === 64) {
        return NonceAccount.Nonce;
    }
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT, {
        accountData: data,
        programName: 'nonce',
    });
}

export enum NonceInstruction {
    Initialize,
    Advance,
}

export function identifyNonceInstruction(
    instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): NonceInstruction {
    const data = 'data' in instruction ? instruction.data : instruction;
    if (containsBytes(data, getU8Encoder().encode(0), 0)) {
        return NonceInstruction.Initialize;
    }
    if (containsBytes(data, getU8Encoder().encode(1), 0)) {
        return NonceInstruction.Advance;
    }
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION, {
        instructionData: data,
        programName: 'nonce',
    });
}

export type ParsedNonceInstruction<TProgram extends string = 'Noncediea1fH12usShuQAz28UhgAeuE5Maf32LsMUQB'> =
    | ({ instructionType: NonceInstruction.Initialize } & ParsedInitializeInstruction<TProgram>)
    | ({ instructionType: NonceInstruction.Advance } & ParsedAdvanceInstruction<TProgram>);

export function parseNonceInstruction<TProgram extends string>(
    instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedNonceInstruction<TProgram> {
    const instructionType = identifyNonceInstruction(instruction);
    switch (instructionType) {
        case NonceInstruction.Initialize: {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: NonceInstruction.Initialize, ...parseInitializeInstruction(instruction) };
        }
        case NonceInstruction.Advance: {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: NonceInstruction.Advance, ...parseAdvanceInstruction(instruction) };
        }
        default:
            throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE, {
                instructionType: instructionType as string,
                programName: 'nonce',
            });
    }
}

export type NoncePlugin = {
    accounts: NoncePluginAccounts;
    instructions: NoncePluginInstructions;
    identifyAccount: typeof identifyNonceAccount;
    identifyInstruction: typeof identifyNonceInstruction;
    parseInstruction: typeof parseNonceInstruction;
};

export type NoncePluginAccounts = { nonce: ReturnType<typeof getNonceCodec> & SelfFetchFunctions<NonceArgs, Nonce> };

export type NoncePluginInstructions = {
    initialize: (input: InitializeInput) => ReturnType<typeof getInitializeInstruction> & SelfPlanAndSendFunctions;
    advance: (input: AdvanceInput) => ReturnType<typeof getAdvanceInstruction> & SelfPlanAndSendFunctions;
};

export type NoncePluginRequirements = ClientWithRpc<GetAccountInfoApi & GetMultipleAccountsApi> &
    ClientWithTransactionPlanning &
    ClientWithTransactionSending;

export function nonceProgram() {
    return <T extends NoncePluginRequirements>(client: T): ExtendedClient<T, { nonce: NoncePlugin }> => {
        return extendClient(client, {
            nonce: <NoncePlugin>{
                accounts: { nonce: addSelfFetchFunctions(client, getNonceCodec()) },
                instructions: {
                    initialize: input => addSelfPlanAndSendFunctions(client, getInitializeInstruction(input)),
                    advance: input => addSelfPlanAndSendFunctions(client, getAdvanceInstruction(input)),
                },
                identifyAccount: identifyNonceAccount,
                identifyInstruction: identifyNonceInstruction,
                parseInstruction: parseNonceInstruction,
            },
        });
    };
}
